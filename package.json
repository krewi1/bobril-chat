{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "tutorial for bobril with socket.io",
  "main": "index.js",
  "scripts": {
    "build": "cd client && bb b -d ../server/dist",
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write **/**/**/**/**/*.{ts,tsx}"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prettier": "^1.18.2"
  },
  "prettier": {
    "printWidth": 140,
    "tabWidth": 4,
    "parser": "typescript"
  }
}
